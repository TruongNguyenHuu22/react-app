import { Paper, Typography } from '@mui/material';

export const ProductReview = () => {
  return (
    <Paper elevation={0} sx={{ padding: '15px' }}>
      <Typography>This is the Review section. Coming soon...</Typography>
    </Paper>
  );
};

export default ProductReview;
